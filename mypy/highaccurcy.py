def longd(dividendo,divisoro):
    dividend=[]
    dividend.extend(str(dividendo))
    divisor=[]
    divisor.extend(str(divisoro))
    lenth=len(divisor)
    cursor=0
    quotient=[]
    print(dividend,divisor)
longd(1,13579)